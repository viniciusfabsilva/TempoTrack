{
    "presets": [
      [
        "babel-preset-vite",
        {
          "env": false, // defaults to true
          "glob": false, // defaults to true
          "hot": false // defaults to true
        }
      ],
      [
        "@babel/preset-env"
      ]
    ]
  }